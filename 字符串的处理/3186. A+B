-------------------------------------------------------------------------------------------------------------
问题：
-------------------------------------------------------------------------------------------------------------
读入两个小于100的非负整数A和B，计算A+B。

需要注意的是：A和B的每一位数字由对应的英文单词给出。

输入格式
第 1 行：整数 T (1≤T≤10) 为测试数据组数。

第 2 ~ T+1 行：每个问题输入一行，格式为：A + B =，相邻两字符串之间用一个空格间隔。

输出格式
对于每个问题，输出一行问题的编号（0 开始编号，格式：case #0: 等），然后对应每个问题在一行中输出A+B的值。
-------------------------------------------------------------------------------------------------------------
样例：
-------------------------------------------------------------------------------------------------------------
输入：
3
three four + five six =
zero seven + eight nine =
zero + zero =

输出：
case #0:
90
case #1:
96
case #2:
0
-------------------------------------------------------------------------------------------------------------
程序：
-------------------------------------------------------------------------------------------------------------
#include <iostream>
using namespace std;

int main() {
	int n;

	cin >> n;
	for (int i = 0; i < n; i++) {
		cout << "case #" << i << ':' << endl;

		/*--------------------------------------------*/

		int k = 0;
		int res = 0;
		int cal[2] = { 0 };
		char a[100];

		while (cin >> a) {  //录入一个词（空格不录入），可能是数字，'+'，'='

			string temp = "";temp = a;
			//temp不等于a，a有无意义字符占据整个100个数组元素，但temp仅仅保留有意义元素
			if (a[0] == '+') { k++;continue; }  //标识事件：录入第一个数 → 录入第二个数
			if (a[0] == '=') break;             //标识事件：录入完成，输出结果res
			
			cal[k] *= 10;                       //标识事件：录入数字的个位（若存在十位）
			if (temp == "zero") cal[k] += 0;
			else if (temp == "one") cal[k] += 1;
			else if (temp == "two") cal[k] += 2;
			else if (temp == "three") cal[k] += 3;
			else if (temp == "four") cal[k] += 4;
			else if (temp == "five") cal[k] += 5;
			else if (temp == "six") cal[k] += 6;
			else if (temp == "seven") cal[k] += 7;
			else if (temp == "eight") cal[k] += 8;
			else if (temp == "nine") cal[k] += 9;
		}

		/*--------------------------------------------*/
		res = cal[0] + cal[1];
		//Output
		cout << res << endl;

		/*--------------------------------------------*/

	}
	return 0;
}

-------------------------------------------------------------------------------------------------------------
注:把one, two...的判定单独设置一个函数可能会更加简便。
